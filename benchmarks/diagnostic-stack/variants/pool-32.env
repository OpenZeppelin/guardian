# What the prod Terraform profile sets today. Included so the current
# production value is a measured data point, not an assumption.
GUARDIAN_DB_POOL_MAX_SIZE=32
GUARDIAN_METADATA_DB_POOL_MAX_SIZE=32
