# Isolates what the fast-track promotion pass contributes to accepted->canonical
# wait. Pair with the canonicalization criterion, not with read latency.
GUARDIAN_CANONICALIZATION_FAST_PROMOTION_ENABLED=false
