# Beyond prod. Issue #317 names the 16-connection default as bottleneck #1 for a
# 20k-reader fan-out; this leg tests whether the gain continues past 32 or
# whether the pool stops being the constraint.
GUARDIAN_DB_POOL_MAX_SIZE=64
GUARDIAN_METADATA_DB_POOL_MAX_SIZE=64
