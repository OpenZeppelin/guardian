# Single replica. Use with: docker compose up -d --scale server-b=0
# GUARDIAN_MAX_REPLICAS must match the running replica count or per-replica
# rate-limit partitioning is computed against a fleet size that does not exist.
GUARDIAN_MAX_REPLICAS=1
