worker_processes 1;
events { worker_connections 64; }

http {
    access_log off;

    server {
        listen 57291;
        http2 on;

        location / {
            return 204;
        }
    }
}
