//! Shared fixtures for `delta_summary` unit tests.
//!
//! Empty placeholder for the new push-time pipeline tests in
//! `build::tests`. Old wrapper-shape JSON fixtures (multisig_*) were
//! removed when the listing pipeline was rewritten to read from the
//! typed `metadata` column instead of decoding at read time.
